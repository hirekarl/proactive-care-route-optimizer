# Copy to .env.local to override. Defaults run the dashboard on built-in mock data.

# Set to "false" to fetch from the live Django REST API instead of mock data.
VITE_USE_MOCK=true

# Base path for API requests (proxied to the Django backend in dev — see vite.config.ts).
VITE_API_BASE=/api
